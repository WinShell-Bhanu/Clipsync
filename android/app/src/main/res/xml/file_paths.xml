<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Restrict to a dedicated subdirectory — never expose the entire cache root -->
    <cache-path name="clipsync_images" path="clipsync_images/" />
</paths>
